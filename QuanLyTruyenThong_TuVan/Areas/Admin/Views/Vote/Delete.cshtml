@model QuanLyTruyenThong_TuVan.Models.ViewModels.VoteViewModel
@{
    ViewData["Title"] = "Xóa biểu quyết";
    Layout = "_LayoutAdmin";
}

<section class="content">
    <div class="container-fluid">
        <div class="card card-danger">
            <div class="card-header">
                <h3 class="card-title">Xóa biểu quyết</h3>
            </div>
            <div class="card-body">
                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger alert-dismissible">
                        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                        <h5><i class="icon fas fa-exclamation-triangle"></i> Lỗi!</h5>
                        @TempData["Error"]
                    </div>
                }
                
                <h4>Bạn có chắc chắn muốn xóa biểu quyết này?</h4>
                <dl class="row">
                    <dt class="col-sm-2">Tiêu đề</dt>
                    <dd class="col-sm-10">@Model.Title</dd>
                    <dt class="col-sm-2">Mô tả</dt>
                    <dd class="col-sm-10">@Model.Description</dd>
                    <dt class="col-sm-2">Ngày bắt đầu</dt>
                    <dd class="col-sm-10">@Model.StartDate.ToString("dd/MM/yyyy")</dd>
                    <dt class="col-sm-2">Ngày kết thúc</dt>
                    <dd class="col-sm-10">@Model.EndDate.ToString("dd/MM/yyyy")</dd>
                    <dt class="col-sm-2">Trạng thái</dt>
                    <dd class="col-sm-10">@Model.Status</dd>
                    <dt class="col-sm-2">Số câu hỏi</dt>
                    <dd class="col-sm-10">@Model.Options.Count</dd>
                </dl>
                <div class="alert alert-warning">
                    <strong>Lưu ý:</strong> Hành động này sẽ xóa biểu quyết và tất cả câu hỏi,kết quả liên quan. Không thể hoàn tác.
                </div>
            </div>
            <div class="card-footer">
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="VoteId" />
                    <button type="submit" class="btn btn-danger">Xóa</button>
                    <a asp-action="Index" class="btn btn-secondary">Hủy</a>
                </form>
            </div>
        </div>
    </div>
</section>